A_volunteer_labeled_data refer to the sensitive data (positive score) which is extracted from dataset version 2, and is cleaned and duplicate tweets are also removed. 

B_volunteer_labeled_data refer to the non-sensitive data (zero scores) which is extracted from dataset version 2, and is cleaned and duplicate tweets are also removed. 